{
  "name": "nodemailer-server",
  "version": "1.0.0",
  "description": "smtp server by nodemailer",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pamento",
  "license": "ISC",
  "dependencies": {
    "smtp-server": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.0"
  }
}
